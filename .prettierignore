.next/
dist/
README.md

# Generated by pnpm, and the only thing standing between `prettier -c .` and a
# green run. Formatting it is also self-defeating: the pre-commit hook rewrites
# it, then the next `pnpm install` rewrites it back.
pnpm-lock.yaml

# Generated by release-please, which does not match Prettier's markdown output:
# it writes `*` list markers and two blank lines after the version heading.
# Verified against its raw output for 0.2.1 -- `prettier -c` flags it.
#
# Without this, every release pull request fails `verify`, which is a required
# check, so the release cannot be merged. That 0.2.1 slipped through was luck:
# the pre-commit hook reformatted the file while an unrelated edit was staged.
CHANGELOG.md
