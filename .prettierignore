.next/
dist/
README.md

# Generated by pnpm, and the only thing standing between `prettier -c .` and a
# green run. Formatting it is also self-defeating: the pre-commit hook rewrites
# it, then the next `pnpm install` rewrites it back.
pnpm-lock.yaml
